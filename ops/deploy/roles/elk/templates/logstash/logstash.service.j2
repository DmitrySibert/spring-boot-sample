[Unit]
Description=Logstash service

[Service]
EnvironmentFile=/etc/environment
User=user
Group=user
Type=simple
ExecStart={{logstash_remote_dest}}/{{ logstash_name }}/bin/logstash

[Install]
WantedBy=multi-user.target